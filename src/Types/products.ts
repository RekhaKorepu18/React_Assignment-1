export type TitemData= {
    
    id:number,
    imageUrl : string,
    name : string,
    price: number,
    rating : number,
    isAvailable : boolean,
    isDiscount?: boolean; 
    discountRate?: number;
  
}